class Collider
{

	/**
	 * Gère les collisions entre plusieurs objets
	 **/
	public do(entities : any) : void
	{
		var vectors : Array = new Array();
		this.entities.forEach(function(entity)
		{

		});
	}
}
